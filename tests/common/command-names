dir=${dir:-.}
get=${get:-${dir}/get}
admin=${admin:-${dir}/admin}
cdc=${cdc:-${dir}/cdc}
prs=${prs:-${dir}/prs}
delta=${delta:-${dir}/delta}
sact=${sact:-${dir}/sact}
sccsdiff=${sccsdiff:-${dir}/sccsdiff}
unget=${unget:-${dir}/unget}
what=${what:-${dir}/what}

DIFF=${DIFF:-diff}
